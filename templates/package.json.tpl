{
  "name": "${project.name}",
  "version": "0.0.0",
  "private": true,
  "description": "Laravel application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ajgenesisnode-model": "*",
    "ajgenesisnode-entity": "*"
  }
}
